package test;

class Test {
	static main() Unit {}
}

// Note: #Self is a type, not a constraint!
// However, #Self is I[#K] rather than I[#Self].
interface I[#K | #K is RequiresI[#Self]] {

}

interface RequiresI[#T | #T is I[#T]] {}
